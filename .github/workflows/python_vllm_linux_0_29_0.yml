name: build python/vllm/linux/0_29_0

on:
  push:
    branches:
      - 'python/vllm/linux/0_29_0'
  pull_request:
    branches:
      - 'python/vllm/linux/0_29_0'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone "$GITHUB_REPOSITORY_OWNER/github_codespaces_compile" source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'python/vllm/linux/0_29_0/get_vllm.sh'
          './python/vllm/linux/0_29_0/get_vllm.sh'
