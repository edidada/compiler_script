name: build cpp/telegram/macos/latest

on:
  push:
    branches:
      - 'cpp/telegram/macos/latest'
  pull_request:
    branches:
      - 'cpp/telegram/macos/latest'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone sandisks/github_codespaces_compile source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'cpp/telegram/macos/latest/get_telegram_mac.sh'
          './cpp/telegram/macos/latest/get_telegram_mac.sh'
