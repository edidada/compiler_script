name: build cpp/telegram_v/linux/latest

on:
  push:
    branches:
      - 'cpp/telegram_v/linux/latest'
  pull_request:
    branches:
      - 'cpp/telegram_v/linux/latest'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone "$GITHUB_REPOSITORY_OWNER/github_codespaces_compile" source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'cpp/telegram_v/linux/latest/get_telegram_linux.sh'
          './cpp/telegram_v/linux/latest/get_telegram_linux.sh'
