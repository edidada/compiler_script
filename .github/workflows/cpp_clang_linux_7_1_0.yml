name: build cpp/clang/linux/7_1_0

on:
  push:
    branches:
      - 'cpp/clang/linux/7_1_0'
  pull_request:
    branches:
      - 'cpp/clang/linux/7_1_0'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone "$GITHUB_REPOSITORY_OWNER/github_codespaces_compile" source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'cpp/clang/linux/7_1_0/get_clang_linux_7_1_0.sh'
          './cpp/clang/linux/7_1_0/get_clang_linux_7_1_0.sh'
