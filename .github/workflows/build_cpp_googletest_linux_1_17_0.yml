name: build cpp/googletest/linux/1_17_0

on:
  push:
    branches:
      - 'cpp/googletest/linux/1_17_0'
  pull_request:
    branches:
      - 'cpp/googletest/linux/1_17_0'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone sandisks/github_codespaces_compile source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'cpp/googletest/linux/1_17_0/get_googletest_linux_1_17_0.sh'
          './cpp/googletest/linux/1_17_0/get_googletest_linux_1_17_0.sh'
