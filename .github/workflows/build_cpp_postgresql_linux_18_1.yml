name: build cpp/postgresql/linux/18_1

on:
  push:
    branches:
      - 'cpp/postgresql/linux/18_1'
  pull_request:
    branches:
      - 'cpp/postgresql/linux/18_1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone sandisks/github_codespaces_compile source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'cpp/postgresql/linux/18_1/get_postgresql.sh'
          './cpp/postgresql/linux/18_1/get_postgresql.sh'
