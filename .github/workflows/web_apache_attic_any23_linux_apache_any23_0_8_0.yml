name: build web/apache_attic_any23/linux/apache_any23_0_8_0

on:
  push:
    branches:
      - 'web/apache_attic_any23/linux/apache_any23_0_8_0'
  pull_request:
    branches:
      - 'web/apache_attic_any23/linux/apache_any23_0_8_0'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone "$GITHUB_REPOSITORY_OWNER/github_codespaces_compile" source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'web/apache_attic_any23/linux/apache_any23_0_8_0/build.sh' 'tools/foundations/build_project.sh'
          './web/apache_attic_any23/linux/apache_any23_0_8_0/build.sh'
