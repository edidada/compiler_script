name: build scala/kafka_v/linux/3_4_0

on:
  push:
    branches:
      - 'scala/kafka_v/linux/3_4_0'
  pull_request:
    branches:
      - 'scala/kafka_v/linux/3_4_0'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone sandisks/github_codespaces_compile source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'scala/kafka_v/linux/3_4_0/get_kafka3_4_0.sh'
          './scala/kafka_v/linux/3_4_0/get_kafka3_4_0.sh'
