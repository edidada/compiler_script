name: build java/derby/linux/latest

on:
  push:
    branches:
      - 'java/derby/linux/latest'
  pull_request:
    branches:
      - 'java/derby/linux/latest'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone sandisks/github_codespaces_compile source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'java/derby/linux/release/get_derby_release.sh'
          './java/derby/linux/release/get_derby_release.sh'
