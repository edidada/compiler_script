name: build cpp/llvm_v/linux/18_1_8

on:
  push:
    branches:
      - 'cpp/llvm_v/linux/18_1_8'
  pull_request:
    branches:
      - 'cpp/llvm_v/linux/18_1_8'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone sandisks/github_codespaces_compile source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'cpp/llvm_v/linux/18_1_8/get_llvm_18_1_8_linux.sh'
          './cpp/llvm_v/linux/18_1_8/get_llvm_18_1_8_linux.sh'
