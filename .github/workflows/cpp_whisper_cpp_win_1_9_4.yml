name: build cpp/whisper_cpp/win/1_9_4

on:
  push:
    branches:
      - 'cpp/whisper_cpp/win/1_9_4'
  pull_request:
    branches:
      - 'cpp/whisper_cpp/win/1_9_4'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: pwsh
        run: |
          gh repo clone "$env:GITHUB_REPOSITORY_OWNER/github_codespaces_compile" source -- --depth=1 --single-branch --branch "$env:GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: pwsh
        run: |
          & './cpp/whisper_cpp/win/1_9_4/get_ai.ps1'
