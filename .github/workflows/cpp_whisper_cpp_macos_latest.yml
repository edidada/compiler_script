name: build cpp/whisper_cpp/macos/latest

on:
  push:
    branches:
      - 'cpp/whisper_cpp/macos/latest'
  pull_request:
    branches:
      - 'cpp/whisper_cpp/macos/latest'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-latest
    timeout-minutes: 360
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone "$GITHUB_REPOSITORY_OWNER/github_codespaces_compile" source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'cpp/whisper_cpp/macos/latest/get_ai.sh'
          './cpp/whisper_cpp/macos/latest/get_ai.sh'
