name: build python/patroni/linux/4_0_0

on:
  push:
    branches:
      - 'python/patroni/linux/4_0_0'
  pull_request:
    branches:
      - 'python/patroni/linux/4_0_0'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone sandisks/github_codespaces_compile source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'python/patroni/linux/4_0_0/get_patroni_4_0_0.sh'
          './python/patroni/linux/4_0_0/get_patroni_4_0_0.sh'
