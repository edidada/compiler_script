name: build rust/serde/macos/latest

on:
  push:
    branches:
      - 'rust/serde/macos/latest'
  pull_request:
    branches:
      - 'rust/serde/macos/latest'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-latest
    timeout-minutes: 360
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone "$GITHUB_REPOSITORY_OWNER/github_codespaces_compile" source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x 'rust/serde/macos/latest/get_rust.sh'
          './rust/serde/macos/latest/get_rust.sh'
