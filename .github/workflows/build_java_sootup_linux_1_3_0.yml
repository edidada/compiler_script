name: build java/sootup/linux/1_3_0

on:
  push:
    branches:
      - 'java/sootup/linux/1_3_0'
  pull_request:
    branches:
      - 'java/sootup/linux/1_3_0'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone sandisks/github_codespaces_compile source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'java/sootup/linux/1_3_0/get_sootup.sh'
          './java/sootup/linux/1_3_0/get_sootup.sh'
