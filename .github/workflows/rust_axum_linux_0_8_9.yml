name: build rust/axum/linux/0_8_9

on:
  push:
    branches:
      - 'rust/axum/linux/0_8_9'
  pull_request:
    branches:
      - 'rust/axum/linux/0_8_9'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone "$GITHUB_REPOSITORY_OWNER/github_codespaces_compile" source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x 'rust/axum/linux/0_8_9/get_rust.sh'
          './rust/axum/linux/0_8_9/get_rust.sh'
