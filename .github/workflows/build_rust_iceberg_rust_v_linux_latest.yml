name: build rust/iceberg_rust_v/linux/latest

on:
  push:
    branches:
      - 'rust/iceberg_rust_v/linux/latest'
  pull_request:
    branches:
      - 'rust/iceberg_rust_v/linux/latest'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone sandisks/github_codespaces_compile source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'rust/iceberg_rust/linux/get_iceberg_rust_linux.sh'
          './rust/iceberg_rust/linux/get_iceberg_rust_linux.sh'
