name: build java/tomcat_v/linux/9

on:
  push:
    branches:
      - 'java/tomcat_v/linux/9'
  pull_request:
    branches:
      - 'java/tomcat_v/linux/9'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone "$GITHUB_REPOSITORY_OWNER/github_codespaces_compile" source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'java/tomcat_v/linux/9/get_tomcat.sh'
          './java/tomcat_v/linux/9/get_tomcat.sh'
          chmod +x -- 'java/tomcat_v/linux/9/get_tomcat9.sh'
          './java/tomcat_v/linux/9/get_tomcat9.sh'
