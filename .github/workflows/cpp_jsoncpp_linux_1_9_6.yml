name: build cpp/jsoncpp/linux/1_9_6

on:
  push:
    branches:
      - 'cpp/jsoncpp/linux/1_9_6'
  pull_request:
    branches:
      - 'cpp/jsoncpp/linux/1_9_6'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone "$GITHUB_REPOSITORY_OWNER/github_codespaces_compile" source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'cpp/jsoncpp/linux/1_9_6/get_jsoncpp_linux_1_9_6.sh'
          './cpp/jsoncpp/linux/1_9_6/get_jsoncpp_linux_1_9_6.sh'
