name: build java/dubbo_v/linux/3_1_11

on:
  push:
    branches:
      - 'java/dubbo_v/linux/3_1_11'
  pull_request:
    branches:
      - 'java/dubbo_v/linux/3_1_11'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone "$GITHUB_REPOSITORY_OWNER/github_codespaces_compile" source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'java/dubbo_v/linux/3_1_11/get_dubbo_3_1_11.sh'
          './java/dubbo_v/linux/3_1_11/get_dubbo_3_1_11.sh'
