name: build cpp/filezilla3_client/linux/latest

on:
  push:
    branches:
      - 'cpp/filezilla3_client/linux/latest'
  pull_request:
    branches:
      - 'cpp/filezilla3_client/linux/latest'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone "$GITHUB_REPOSITORY_OWNER/github_codespaces_compile" source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'cpp/filezilla3_client/linux/latest/get_filezilla3_linux_client.sh'
          './cpp/filezilla3_client/linux/latest/get_filezilla3_linux_client.sh'
