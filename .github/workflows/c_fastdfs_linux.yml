name: build fastdfs 6.08

on:
  push:
    branches:
      - 'c/fastdfs_v/linux/6_08'
  pull_request:
    branches:
      - 'c/fastdfs_v/linux/6_08'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Clone matching private source branch
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        run: |
          set -euo pipefail
          gh repo clone sandisks/github_codespaces_compile private-source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"

      - name: Execute source script
        shell: bash
        run: |
          set -euo pipefail
          test -f 'private-source/c/fastdfs/linux/get_fastdfs.sh'
          chmod +x 'private-source/c/fastdfs/linux/get_fastdfs.sh'
          'private-source/c/fastdfs/linux/get_fastdfs.sh'
