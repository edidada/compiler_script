name: build cpp/brpc/linux/0_9_7

on:
  push:
    branches:
      - 'cpp/brpc/linux/0_9_7'
  pull_request:
    branches:
      - 'cpp/brpc/linux/0_9_7'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Clone matching private source branch
        env:
          GH_TOKEN: ${{ secrets.COMPILER_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          gh repo clone "$GITHUB_REPOSITORY_OWNER/github_codespaces_compile" source -- --depth=1 --single-branch --branch "$GITHUB_REF_NAME"
      - name: Execute source script
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          chmod +x -- 'cpp/brpc/linux/0_9_7/get_brpc_0_9_7.sh'
          './cpp/brpc/linux/0_9_7/get_brpc_0_9_7.sh'
